# A simple game to manipulate the ball..

# first we need to make the simulator and the world body (which is the register
# we need define
from .simulator import Simulator

class Demo2D(Simulator):

    def build_renderer(self):
        pass
